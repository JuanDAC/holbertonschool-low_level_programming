#include "holberton.h"
#include <stdio.h>
/**
* _strspn - Entry point
* @s: char
* @accept: char
* Return: Always 0 (Success)
*/
unsigned int _strspn(char *s, char *accept)
{
	unsigned acumulator = 0;

	s = (s + acumulator);
	accept = (accept + acumulator);

	return (acumulator);
}

