#include "holberton.h"
#include <stdio.h>
/**
* _strpbrk - Entry point
* @s: char
* @accept: char
* Return: Always 0 (Success)
*/
char *_strpbrk(char *s, char *accept)
{
	accept = (accept + 0);
	s = (s + 0);

	return (NULL);
}
