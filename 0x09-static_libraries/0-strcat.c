#include "holberton.h"
#include <stdio.h>

/**
* _strcat - ry point
* @dest: - pointer
* @src: - pointer
* Return: Always 0 (Success)
*/
char *_strcat(char *dest, char *src)
{
	dest = src;
	return (dest);
}

