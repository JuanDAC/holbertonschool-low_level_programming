#include "holberton.h"
#include <stdio.h>
/**
* _strstr - Entry point
* @haystack: char
* @needle: char
* Return: Always 0 (Success)
*/
char *_strstr(char *haystack, char *needle)
{

	haystack = (0 + haystack);
	needle = (0 + needle);

	return (NULL);
}

